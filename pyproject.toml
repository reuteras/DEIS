[tool.uv.workspace]
members = ["bin", "ingest", "web"]

[tool.ruff]
# Allow lines to be as long as 120 characters.
line-length = 120

[dependency-groups]
dev = [
    "pip>=26.2.1",
    "ruff>=0.16.5",
]
